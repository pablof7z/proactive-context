---
import type { RefusalItem } from "@data/home";

interface Props {
  item: RefusalItem;
}

const { item } = Astro.props;
---

<article class={`refusal-card surface is-${item.tone}`} data-reveal>
  <span class="refusal-mark">⊘</span>
  <h3>{item.title}</h3>
  <p>{item.body}</p>
</article>
