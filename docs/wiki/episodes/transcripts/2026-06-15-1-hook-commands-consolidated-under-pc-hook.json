[
  [
    "user",
    "stuff that is to be called via a hook should work like in ~/src/tenex-edge via a hook subcommand (i.e. instead of pc capture or pc session-start)\n\nwith a --harness <claude|opencode|codex> if its important to disambiguiate behavior of the different harnesses"
  ],
  [
    "assistant",
    "Added `Hook` subcommand with `Capture`, `Inject`, `SessionStart`, `Statusline` subcommands and `--harness` flag, updated all wirings to use the `hook` form, and retained old top-level commands for backward compatibility."
  ],
  [
    "user",
    "no backwards compatibility ffs"
  ],
  [
    "assistant",
    "Removed old top-level commands; everything now lives under `pc hook`."
  ],
  [
    "user",
    "commit and push"
  ],
  [
    "assistant",
    "Pushed. Two commits landed: `00aa282` (hook subcommand + wiring updates) and `44def50` (old top-level commands removed)."
  ]
]