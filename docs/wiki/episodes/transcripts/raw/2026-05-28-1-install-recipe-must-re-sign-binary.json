[
  [
    "user",
    "~/.bin/proactive-context\n[1]    96395 killed     ~/.bin/proactive-context"
  ],
  [
    "assistant",
    "`~/.bin/proactive-context` is working again. Added `codesign --force --sign -` to the `just install` recipe so future installs won't hit this again."
  ]
]