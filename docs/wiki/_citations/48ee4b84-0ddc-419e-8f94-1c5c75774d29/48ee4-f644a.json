{
  "id": "48ee4-f644a",
  "ts": "2026-06-29T12:33:31Z",
  "session": "48ee4b84-0ddc-419e-8f94-1c5c75774d29",
  "text": "## 3. Substance gate — whole-session veto (capture.rs:2257)\n- `exchanges` = **count of user turns** (not adjacency pairs — that was the old bug that dropped tool-heavy sessions).\n- **Skip the session** if `plain_ts.len() < 500` chars **OR** `exchanges < 1`. Deliberately permissive — triage (a separate LLM call) is meant to be the real \"is there a lesson?\" filter. [...] ## 4. Dedup (capture.rs:2233)\n`is_already_captured_in(session_id, exchanges)` — skip if a capture marker already exists for this session. [...] ## 5. Truncation — tail-keep, two different limits\n- **Triage input (`plain_ts`)**: if > **200,000 chars**, keep the **last 200k** (capture.rs:2246).\n- **EXTRACT input (`numbered_transcript`)**: if > **250,000 chars**, keep the **last 250k** (capture.rs:2355). [...] **`src/capture.rs`** — both truncation sites:\n- Triage input: `reduce_turns_to_fit(&turns, 200_000, false)` + `tail_capped` backstop.\n- EXTRACT input: `reduce_turns_to_fit(&turns, 250_000, true)`, then `numbered_transcript` / `transcript_lines` / `transcript_roles` all built from that **same reduced set** — preserving the absolute-line-number invariant that `evidence_is_valid` / `author_for_ranges` / `cite` depend on. Confirmed those are the only three consumers."
}
