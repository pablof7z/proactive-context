{
  "id": "9c66c-cf055",
  "ts": "2026-06-29T12:16:01Z",
  "session": "9c66cf85-c0e0-4e36-aa6f-c802d8cb0efd",
  "text": "in all those 2m I didn't see a single log update, I had no idea what the thing was doing -- I want it to tell me what its going on more proactively [...] Assistant: Now render the stage in the \"current\" region:\n [...] 1. **Live phase in the headline** — the \"current\" box now narrates the actual stage (`starting` → `extracting claims` → `tagging authority` → `routing to guides` → `reconciling guides` → `writing wiki` → `rebuilding index`), driven by the session's own events, with a `· waiting on model` marker between an `llm.request` and its response. This is the piece that stays informative *through* a silent LLM call. [...]      - `CurrentSession` struct — added `stage: String` and `waiting_on_model: bool`.\n     - New helper `stage_label_for_event(event: &str) -> Option<&'static str>` mapping: capture.start→\"extracting claims\", capture.extract→\"tagging authority\", capture.authority_tagging/route_recall→\"routing to guides\", capture.route→\"reconciling guides\", wiki.*→\"writing wiki\", capture.agent_done→\"rebuilding index\".\n     - SessionStart handler in `run_tui_mode` — initialized new fields: `stage: \"starting\".to_string(), waiting_on_model: false`. [...]        ``` [...]    - **User feedback via advisor:** (1) Don't hardcode \"triaging…\" as initial label (triage is conditional) — used \"starting\" instead. (2) The summary still misreports a q-interrupted session — fixed via trailing-event drain after worker.join()."
}
