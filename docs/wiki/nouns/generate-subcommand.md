---
type: noun-entry
slug: generate-subcommand
name: "generate subcommand"
origin: extracted
source_refs:
  - transcript:2237-2244
---

# generate subcommand

A removed manual CLI command (`pc generate "question"`) that answered from the user's notes using a multi-turn agent with a read_file tool. Its model roles (generate_model, decompose_model) have been removed from configuration; inject is now the sole retrieval interface.
