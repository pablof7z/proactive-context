---
type: noun-entry
slug: status-bar-indicator-command
name: "status-bar indicator command"
origin: extracted
source_refs:
  - transcript:52-55
---

# status-bar indicator command

Renders a one-line Claude Code status bar indicator, invoked via statusLine.command; reads Claude Code status-line JSON from stdin and prints one styled line to stdout, always exits 0 with no LLM, no network, sub-10ms.
